# tasks.def — tasks for integration tests

c {
    #include "lace.h"
}

rust {
}

types {
}

task fib(n: i32) -> i32
task nqueens(a: *const i32, n: i32, d: i32, i: i32) -> i64
task uts_test(state: *const u8, depth: i32) -> i64
