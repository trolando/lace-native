[package]
name = "lace-tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
doc = false

[dependencies]
lace-native = { path = "../../lace-native" }
lace-example-uts = { path = "../../examples/uts" }

[build-dependencies]
lace-native-build = { path = "../../lace-native-build" }
